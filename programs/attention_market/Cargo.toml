[package]
name = "attention_market"
version = "0.1.0"
description = "CrowdCast attention prediction market program"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "attention_market"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = { version = "0.29.0", features = ["init-if-needed"] }
